// import dotenv from "dotenv"

// dotenv.config()

// const PORT = process.env.PORT || 2134;
const PORT = 2134;

export const SERVER = `http://localhost:${PORT}/api/v1`;
export const LOCALRELAY = "ws://localhost:8008";
